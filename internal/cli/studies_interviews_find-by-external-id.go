// Copyright 2026 francis and contributors. Licensed under Apache-2.0. See LICENSE.
// Generated by CLI Printing Press (https://github.com/mvanhorn/cli-printing-press). DO NOT EDIT.

package cli

import (
	"encoding/json"
	"fmt"
	"os"

	"github.com/spf13/cobra"
)

func newStudiesInterviewsFindByExternalIdCmd(flags *rootFlags) *cobra.Command {
	var flagExternalParticipantId string
	var flagIncludeSimulated bool
	var flagLimit int
	var flagAll bool

	cmd := &cobra.Command{
		Use:         "find-by-external-id <study_id>",
		Aliases:     []string{"get"},
		Short:       "Every interview in this study that arrived carrying ``external_participant_id`` — your own id for the person",
		Example:     "  deutero-pp-cli studies interviews find-by-external-id f0ed9214-06fc-4420-87b3-31240b670fc1 --external-participant-id panel-42",
		Annotations: map[string]string{"pp:endpoint": "interviews.find-by-external-id", "pp:method": "GET", "pp:path": "/api/v1/studies/{study_id}/interviews/by-external-id", "mcp:read-only": "true", "pp:requires-input": "true", "pp:happy-args": "study_id=f0ed9214-06fc-4420-87b3-31240b670fc1;--external-participant-id=panel-42"},
		RunE: func(cmd *cobra.Command, args []string) error {
			// Bare invocation of a command with required input prints help
			// instead of pflag's terse "required flag not set" error. Optional-
			// only read commands fall through so a bare call still executes.
			// Machine callers (--json/--agent, which sets asJSON) get a usage
			// error + exit 2 instead of silent exit-0 help, so an incomplete
			// invocation is never mistaken for success.
			if !hasChangedLocalFlags(cmd) && len(args) == 0 && !flags.dryRun {
				if flags.asJSON {
					if printErr := printJSONFiltered(cmd.OutOrStdout(), map[string]any{
						"error": "requires input",
						"usage": cmd.CommandPath() + " --help",
					}, flags); printErr != nil {
						return printErr
					}
					return usageErr(fmt.Errorf("%q requires input; run %q for usage", cmd.CommandPath(), cmd.CommandPath()+" --help"))
				}
				return cmd.Help()
			}
			if len(args) == 0 {
				// A missing required positional is a usage error in every output
				// mode (matches command_promoted.go.tmpl). Machine callers
				// (--json/--agent) also get a JSON error envelope on stdout;
				// usageErr sets exit 2.
				if flags.asJSON {
					if printErr := printJSONFiltered(cmd.OutOrStdout(), map[string]any{
						"error": "missing required argument",
						"usage": fmt.Sprintf("%s%s", cmd.CommandPath(), " <study_id>"),
					}, flags); printErr != nil {
						return printErr
					}
				}
				return usageErr(fmt.Errorf("missing required argument\nUsage: %s%s", cmd.CommandPath(), " <study_id>"))
			}
			if !cmd.Flags().Changed("external-participant-id") && flagExternalParticipantId == "" && !flags.dryRun {
				return fmt.Errorf("required flag \"%s\" not set", "external-participant-id")
			}
			path := "/api/v1/studies/{study_id}/interviews/by-external-id"
			if len(args) < 1 || args[0] == "" {
				return usageErr(fmt.Errorf("study_id is required\nUsage: %s <%s>", cmd.CommandPath(), "study_id"))
			}
			path = replacePathParam(path, "study_id", args[0])
			c, err := flags.newClient()
			if err != nil {
				return err
			}
			data, prov, err := resolvePaginatedReadWithStrategy(cmd.Context(), c, flags, "live", "interviews", path, retainCLIQueryParams(cmd, map[string]string{
				"external_participant_id": formatCLIParamValue(flagExternalParticipantId),
				"include_simulated":       formatCLIParamValue(flagIncludeSimulated),
				"limit":                   formatCLIParamValue(flagLimit),
			}, map[string][]string{"external_participant_id": {"external-participant-id"}, "include_simulated": {"include-simulated"}, "limit": {"limit"}}, "", "offset"), nil, flagAll, "", "offset", "limit", 20, "", "", "interviews", cmd.ErrOrStderr())
			if err != nil {
				return classifyAPIError(cmd.OutOrStdout(), err, flags)
			}
			outputData := collectionItemsForOutput(data, path)
			// Print provenance to stderr for human-facing output only.
			// Machine-format flags (--json, --csv, --compact, --quiet, --plain,
			// --select) and piped stdout suppress this line; the JSON envelope
			// already carries meta.source for those consumers.
			// SYNC: keep this gate aligned with command_promoted.go.tmpl.
			if wantsHumanTable(cmd.OutOrStdout(), flags) {
				var countItems []json.RawMessage
				_ = json.Unmarshal(outputData, &countItems)
				printProvenance(cmd, len(countItems), prov)
			}
			// For JSON output, wrap with provenance envelope before passing through flags.
			// --select wins over --compact when both are set; --compact only runs when
			// no explicit fields were requested. Explicit format flags (--csv, --quiet,
			// --plain) opt out of the auto-JSON path so piped consumers that asked for
			// a non-JSON format reach the standard pipeline below.
			if flags.asJSON || (!isTerminal(cmd.OutOrStdout()) && !flags.csv && !flags.quiet && !flags.plain) {
				var selectErr error
				filtered := data
				if flags.selectFields != "" {
					filtered, selectErr = filterFieldsChecked(filtered, flags.selectFields)
				} else if flags.compact {
					filtered = compactFields(filtered, map[string]bool{"external_participant_id": true, "id": true, "message_count": true, "participant_id": true, "participant_name": true, "study_id": true})
				}
				wrapped, wrapErr := wrapWithProvenance(filtered, prov)
				if wrapErr != nil {
					return wrapErr
				}
				wrapped, wrapErr = wrapPlatformStructuredOutput(wrapped, flags, "results", true)
				if wrapErr != nil {
					return wrapErr
				}
				if err := printOutput(cmd.OutOrStdout(), wrapped, true); err != nil {
					return err
				}
				return selectErr
			}
			// For all other output modes (table, csv, plain, quiet), use the standard pipeline
			if wantsHumanTable(cmd.OutOrStdout(), flags) {
				var items []map[string]any
				if json.Unmarshal(outputData, &items) == nil && len(items) > 0 {
					if err := printAutoTable(cmd.OutOrStdout(), items); err != nil {
						return err
					}
					if len(items) >= 25 {
						fmt.Fprintf(os.Stderr, "\nShowing %d results. To narrow: add --limit, --json --select, or filter flags.\n", len(items))
					}
					return nil
				}
			}
			formatData := data
			if flags.csv || flags.plain {
				formatData = outputData
			}
			return printOutputWithFlagsMeta(cmd.OutOrStdout(), formatData, flags, map[string]any{"source": "live"}, map[string]bool{"external_participant_id": true, "id": true, "message_count": true, "participant_id": true, "participant_name": true, "study_id": true})
		},
	}
	cmd.Flags().StringVar(&flagExternalParticipantId, "external-participant-id", "", "Your own id for the participant, exactly as the entry link or embed metadata carried it")
	cmd.Flags().BoolVar(&flagIncludeSimulated, "include-simulated", false, "Include simulated (persona) interviews")
	cmd.Flags().IntVar(&flagLimit, "limit", 20, "Most recent N attempts")
	cmd.Flags().BoolVar(&flagAll, "all", false, "Fetch all pages")

	return cmd
}
